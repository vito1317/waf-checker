{
	"name": "proud-haze-2d74",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build:css": "tailwindcss -i src/tailwind-input.css -o src/static/tailwind.css --minify",
		"dev:css": "tailwindcss -i src/tailwind-input.css -o src/static/tailwind.css --watch",
		"deploy": "npm run build:css && wrangler deploy",
		"dev": "npm run build:css && wrangler dev",
		"start": "npm run build:css && wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"tailwindcss": "^3.4.19",
		"typescript": "^5.5.2",
		"vitest": "~3.0.7",
		"wrangler": "^4.17.0"
	}
}
